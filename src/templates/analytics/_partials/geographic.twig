{# Geographic Tab - Countries and Cities (loaded via AJAX) #}

{% set settings = craft.app.plugins.getPlugin('shortlink-manager').settings %}

{% if settings.enableGeoDetection %}
    <h2 class="lr-section-heading">{{ "Geographic Distribution"|t('shortlink-manager') }}</h2>

    <div class="lr-analytics-charts two-columns">
        <div class="lr-chart-container">
            <h3>{{ "Top Countries"|t('shortlink-manager') }}</h3>
            <div class="lr-table-scroll">
                <table class="data fullwidth">
                    <thead>
                        <tr>
                            <th>{{ "Country"|t('shortlink-manager') }}</th>
                            <th>{{ "Interactions"|t('shortlink-manager') }}</th>
                            <th>{{ "Percentage"|t('shortlink-manager') }}</th>
                        </tr>
                    </thead>
                    <tbody id="top-countries-body">
                        <tr>
                            <td colspan="3" class="light lr-text-center">
                                <div class="spinner"></div>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="lr-chart-container">
            <h3>{{ "Top Cities"|t('shortlink-manager') }}</h3>
            <div class="lr-table-scroll">
                <table class="data fullwidth">
                    <thead>
                        <tr>
                            <th>{{ "City"|t('shortlink-manager') }}</th>
                            <th>{{ "Country"|t('shortlink-manager') }}</th>
                            <th>{{ "Interactions"|t('shortlink-manager') }}</th>
                            <th>{{ "Percentage"|t('shortlink-manager') }}</th>
                        </tr>
                    </thead>
                    <tbody id="top-cities-body">
                        <tr>
                            <td colspan="4" class="light lr-text-center">
                                <div class="spinner"></div>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% else %}
    <div class="pane lr-analytics-empty">
        <p>{{ "Geographic detection is disabled."|t('shortlink-manager') }}</p>
        <p><a href="{{ url('shortlink-manager/settings/analytics') }}" class="btn">{{ "Enable in Settings"|t('shortlink-manager') }}</a></p>
    </div>
{% endif %}
