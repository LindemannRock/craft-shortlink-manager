#!/bin/bash

if ! command -v ddev >/dev/null 2>&1; then
    echo 'ShortLink Manager pre-commit requires DDEV. Install DDEV and ensure it is on PATH, then run: ddev exec "cd plugins/shortlink-manager && composer ci"' >&2
    exit 127
fi

exec ddev exec "cd plugins/shortlink-manager && composer ci"
